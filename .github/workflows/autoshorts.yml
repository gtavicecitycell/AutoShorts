name: Auto YouTube Shorts

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"

      - name: Upgrade pip
        run: python -m pip install --upgrade pip

      - name: Install dependencies
        run: |
          pip install --upgrade setuptools wheel
          pip install openai google-auth google-auth-oauthlib google-auth-httplib2 google-api-python-client Pillow requests moviepy

      - name: Check moviepy installation
        run: |
          python -c "import moviepy.editor; print('✅ MoviePy installed correctly!')"

      - name: Run Auto Shorts Script
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          YOUTUBE_API_KEY: ${{ secrets.YOUTUBE_API_KEY }}
        run: python autoshorts.py
 
